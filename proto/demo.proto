syntax = "proto3";

package demo;

message Request {
  string name = 1;
  string text = 2;
  int32 count = 3;
  int64 startedAt = 4;
}

message Reply {
  string message = 1;
}

service Service {
  rpc BidiStream (stream Request) returns (stream Reply);
}
